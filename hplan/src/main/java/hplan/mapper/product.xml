<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="product">
<select id="list" resultType="hplan.dto.ProductDTO">
select * from product
order by product_id desc
</select>
<insert id="product_insert">
insert into product(product_id, p_name, p_price ,p_info ,p_stock,pf_img,cate_name)
values (product_seq.nextval, #{p_name},#{p_price},#{p_info},#{p_stock},#{pf_img},#{cate_name})

</insert>
<select id="product_view" resultType="hplan.dto.ProductDTO">
select * from product
where product_id=#{product_id}
</select>
<select id="product_priceSum" resultType="int">
select nvl(sum(p_price*p_stock-p_sell),0) sum
from product
where product_id = #{product_id}

</select>
</mapper>